import { DiDirective } from './di.directive';

describe('DiDirective', () => {
  it('should create an instance', () => {
    const directive = new DiDirective();
    expect(directive).toBeTruthy();
  });
});
